# 司马八字 · 项目部署手册
> 最后更新：2026-07-29

---

## 一、项目概览

| 项目 | 用途 | GitHub | 状态 |
|------|------|--------|------|
| simabazi-api | 旧版PWA后端（八字+AI聊天+水晶） | jacklam1006/simabazi-api | Free 睡眠中 |
| simabazi-game | 新版3D游戏前端 | jacklam1006/simabazi-game | 开发中 |
| simabazi-island | 新版3D岛屿生成后端 | jacklam1006/simabazi-game (子目录) | 待部署 |

---

## 二、新项目部署（simabazi-game）

### 2.1 后端：simabazi-island（Render）

**第一次部署**：
1. render.com → New → Web Service
2. 连接仓库：`jacklam1006/simabazi-game`
3. Render 自动读取 `render.yaml`，服务名 `simabazi-island`
4. Instance Type 选 **Starter（$7/month）**
5. 点 Deploy

**必须填入的环境变量**（Render → Environment）：

| 变量名 | 来源 | 备注 |
|--------|------|------|
| `GEMINI_API_KEY` | aistudio.google.com → Get API key | ⚠️ 每次泄露后需重新生成 |
| `TRIPO_API_KEY` | tripo3d.ai 或 developers.tripo3d.com（国内）| ⚠️ 每次泄露后需重新生成 |
| `GEMINI_IMAGE_MODEL` | 已在 render.yaml 写死，无需手动填 | gemini-2.0-flash-preview-image-generation |
| `GEMINI_ANALYSIS_MODEL` | 选填，用于覆盖AI深析（`gemini_analysis.py`）使用的模型 | 不设置则使用代码内置模型回退链：`gemini-flash-latest` → `gemini-2.5-flash` → `gemini-2.0-flash` |
| `GEMINI_ENHANCE_MODEL` | 选填，用于覆盖提示词增强（`gemini_enhance.py`）使用的模型 | 不设置则使用代码内置模型回退链：`gemini-flash-latest` → `gemini-2.5-flash` → `gemini-2.0-flash` |

**重新部署**（代码更新后）：
- 推送到 GitHub 自动触发，或 Render → Manual Deploy

### 2.2 前端：simabazi-game（Vercel）

1. vercel.com → New Project → 连接 `jacklam1006/simabazi-game`
2. 无需额外配置，`vercel.json` 已设置好 SPA 路由
3. 点 Deploy，自动获得 `.vercel.app` 域名

---

## 三、旧项目重新激活（simabazi-api）

**当前状态**：Render Free 套餐，服务睡眠，代码和数据库完整

**重新激活步骤（约5分钟）**：
1. render.com → simabazi-api → Scaling → 升级到 **Starter（$7/month）**
2. Manual Deploy → 点击部署
3. 等待构建（约3-4分钟）
   - 自动安装依赖
   - 自动运行 `python ingest_knowledge.py` 重建 AI 知识库（无需手动操作）
4. 完成，`api.bazicalculatefree.com` 恢复服务

**旧项目环境变量**（Render 已保存，升级后自动恢复）：

| 变量名 | 说明 |
|--------|------|
| `SECRET_KEY` | Render 自动生成 |
| `SUPABASE_URL` | Supabase 项目地址 |
| `SUPABASE_ANON_KEY` | Supabase 匿名密钥 |
| `SUPABASE_SERVICE_KEY` | Supabase 服务密钥 |
| `GEMINI_API_KEY` | Google AI Studio |
| `SMTP_USER` | Hostinger 邮箱账号 |
| `SMTP_PASS` | Hostinger 邮箱密码 |
| `ADMIN_EMAIL` | ngoliangfong@gmail.com |

---

## 四、本地开发环境

**启动开发服务器**（实时预览）：
```bash
# 方法一：双击文件
双击 ~/Desktop/simabazi-game/启动开发服务器.command

# 方法二：终端
cd ~/Desktop/simabazi-game
npx live-server --port=3000
```
浏览器打开 http://localhost:3000

**Chrome 注意**：DevTools → Application → Service Workers → 勾选 **Update on reload**，避免缓存干扰

---

## 五、关键 API 端点

### simabazi-island（新版后端）
| 端点 | 说明 |
|------|------|
| `POST /generate` | 提交岛屿生成任务，返回 job_id |
| `GET /status/{job_id}` | 轮询进度（前端每3秒调用） |
| `GET /health` | 检查 API Key 是否已配置 |
| `GET /ping` | 保活端点（前端每14分钟自动调用） |

### 生成流水线
```
用户八字 → bazi_prompt.py（生成提示词）
        → Gemini（生成2D岛屿图像）
        → TripoAI image-to-3D（图生3D，主路径）
        → 失败自动降级 → TripoAI text-to-3D（文生3D，兜底）
        → GLB 模型 URL → 前端 Three.js 加载展示
```

---

## 六、已知修复记录（2026-07-26）

| 文件 | 问题 | 修复内容 |
|------|------|---------|
| `island_service/tripo_client.py` | 字段名错误 | `file_token` → `image_token` |
| `island_service/main.py` | 字段名错误 | `model_url` → `model` |
| `island_service/bazi_prompt.py` | 空亡崩溃 | kongwang 从 dict 改为 array 处理 |
| `js/tasks.js` | 每日任务无特效 | 补充 UIEffects/AudioManager 直接调用 |
| `js/main-new.js` | 后端冷启动慢 | 加入 /ping 保活，页面加载即触发 |

---

## 七、安全提醒

⚠️ **API Key 不要在任何聊天工具里发送**，包括 Claude。

如果 key 已经泄露，立即：
- Google AI Studio → 进入对应 key → 删除 → 重新创建
- tripo3d.ai → API Keys → 删除 → 重新创建
- 然后更新 Render 环境变量

---

## 八、费用概览

| 服务 | 套餐 | 月费 |
|------|------|------|
| simabazi-island（Render） | Starter | $7 |
| simabazi-api（Render，睡眠中） | Free | $0 |
| Vercel 前端 | Free | $0 |
| Google AI Studio | 按量付费 | 视用量 |
| TripoAI | 按量付费 | 视用量 |
| **合计** | | **$7 + API用量** |
